// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'dart:io';

final class CliEnvValues {
  const CliEnvValues({required this.apiBase, required this.flags2envConfig, required this.happyWakeyAccessToken, required this.pretty, required this.sharedAuthBase, required this.enabled, required this.gradualSeconds, required this.label, required this.localTime, required this.sound, required this.tags, required this.timeZone, required this.transitionId, required this.volume, required this.weekdays, required this.clientTime, required this.event, required this.expectedGeneration, required this.occurrenceId, required this.snoozeUntil});

  final String apiBase;
  final String? flags2envConfig;
  final String? happyWakeyAccessToken;
  final bool pretty;
  final String sharedAuthBase;
  final bool enabled;
  final int gradualSeconds;
  final String? label;
  final String? localTime;
  final String sound;
  final String? tags;
  final String? timeZone;
  final String? transitionId;
  final String volume;
  final String? weekdays;
  final String? clientTime;
  final String? event;
  final int? expectedGeneration;
  final String? occurrenceId;
  final String? snoozeUntil;
}

/// Pure: resolve values from an explicit lookup.
CliEnvValues loadFrom(String? Function(String key) lookup) {
  return CliEnvValues(
    apiBase: _nonEmpty(lookup('HAPPY_WAKEY_API_BASE')) ?? 'https://api.happy-wakey.dev',
    flags2envConfig: _nonEmpty(lookup('FLAGS2ENV_CONFIG')),
    happyWakeyAccessToken: _nonEmpty(lookup('HAPPY_WAKEY_ACCESS_TOKEN')),
    pretty: _parseBool(lookup('HAPPY_WAKEY_PRETTY'), false),
    sharedAuthBase: _nonEmpty(lookup('HAPPY_WAKEY_SHARED_AUTH_BASE')) ?? 'https://auth.oresoftware.dev',
    enabled: _parseBool(lookup('HAPPY_WAKEY_ALARM_ENABLED'), true),
    gradualSeconds: _parseInt(lookup('HAPPY_WAKEY_ALARM_GRADUAL_SECONDS'), 30),
    label: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_LABEL')),
    localTime: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_LOCAL_TIME')),
    sound: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_SOUND')) ?? 'bell',
    tags: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_TAGS')),
    timeZone: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_TIME_ZONE')),
    transitionId: _nonEmpty(lookup('HAPPY_WAKEY_TRANSITION_ID')),
    volume: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_VOLUME')) ?? '0.8',
    weekdays: _nonEmpty(lookup('HAPPY_WAKEY_ALARM_WEEKDAYS')),
    clientTime: _nonEmpty(lookup('HAPPY_WAKEY_CLIENT_TIME')),
    event: _nonEmpty(lookup('HAPPY_WAKEY_TRANSITION_EVENT')),
    expectedGeneration: int.tryParse(lookup('HAPPY_WAKEY_EXPECTED_GENERATION') ?? ''),
    occurrenceId: _nonEmpty(lookup('HAPPY_WAKEY_OCCURRENCE_ID')),
    snoozeUntil: _nonEmpty(lookup('HAPPY_WAKEY_SNOOZE_UNTIL')),
  );
}

/// Resolve values from a map of env keys.
CliEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
CliEnvValues loadFromOs() {
  return loadFromMap(Platform.environment);
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

bool _parseBool(String? raw, bool fallback) {
  switch (raw?.trim()) {
    case '1':
    case 'true':
    case 'TRUE':
    case 'yes':
    case 'YES':
      return true;
    case '0':
    case 'false':
    case 'FALSE':
    case 'no':
    case 'NO':
      return false;
    default:
      return fallback;
  }
}

int _parseInt(String? raw, int fallback) {
  return int.tryParse(raw?.trim() ?? '') ?? fallback;
}

double _parseDouble(String? raw, double fallback) {
  return double.tryParse(raw?.trim() ?? '') ?? fallback;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (key.isEmpty) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = Platform.environment['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.fold<Map<String, String>>({}, (acc, path) {
    try {
      acc.addAll(parseDotenv(File(path).readAsStringSync()));
    } catch (_) {}
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final apiBase = pick(['HAPPY_WAKEY_API_BASE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'https://api.happy-wakey.dev');
  if (apiBase != null) out['HAPPY_WAKEY_API_BASE'] = apiBase;
  final flags2envConfig = pick(['FLAGS2ENV_CONFIG'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (flags2envConfig != null) out['FLAGS2ENV_CONFIG'] = flags2envConfig;
  final happyWakeyAccessToken = pick(['HAPPY_WAKEY_ACCESS_TOKEN'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (happyWakeyAccessToken != null) out['HAPPY_WAKEY_ACCESS_TOKEN'] = happyWakeyAccessToken;
  final pretty = pick(['HAPPY_WAKEY_PRETTY'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (pretty != null) out['HAPPY_WAKEY_PRETTY'] = pretty;
  final sharedAuthBase = pick(['HAPPY_WAKEY_SHARED_AUTH_BASE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'https://auth.oresoftware.dev');
  if (sharedAuthBase != null) out['HAPPY_WAKEY_SHARED_AUTH_BASE'] = sharedAuthBase;
  final enabled = pick(['HAPPY_WAKEY_ALARM_ENABLED'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'true');
  if (enabled != null) out['HAPPY_WAKEY_ALARM_ENABLED'] = enabled;
  final gradualSeconds = pick(['HAPPY_WAKEY_ALARM_GRADUAL_SECONDS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '30');
  if (gradualSeconds != null) out['HAPPY_WAKEY_ALARM_GRADUAL_SECONDS'] = gradualSeconds;
  final label = pick(['HAPPY_WAKEY_ALARM_LABEL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (label != null) out['HAPPY_WAKEY_ALARM_LABEL'] = label;
  final localTime = pick(['HAPPY_WAKEY_ALARM_LOCAL_TIME'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (localTime != null) out['HAPPY_WAKEY_ALARM_LOCAL_TIME'] = localTime;
  final sound = pick(['HAPPY_WAKEY_ALARM_SOUND'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'bell');
  if (sound != null) out['HAPPY_WAKEY_ALARM_SOUND'] = sound;
  final tags = pick(['HAPPY_WAKEY_ALARM_TAGS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (tags != null) out['HAPPY_WAKEY_ALARM_TAGS'] = tags;
  final timeZone = pick(['HAPPY_WAKEY_ALARM_TIME_ZONE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (timeZone != null) out['HAPPY_WAKEY_ALARM_TIME_ZONE'] = timeZone;
  final transitionId = pick(['HAPPY_WAKEY_TRANSITION_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (transitionId != null) out['HAPPY_WAKEY_TRANSITION_ID'] = transitionId;
  final volume = pick(['HAPPY_WAKEY_ALARM_VOLUME'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '0.8');
  if (volume != null) out['HAPPY_WAKEY_ALARM_VOLUME'] = volume;
  final weekdays = pick(['HAPPY_WAKEY_ALARM_WEEKDAYS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (weekdays != null) out['HAPPY_WAKEY_ALARM_WEEKDAYS'] = weekdays;
  final clientTime = pick(['HAPPY_WAKEY_CLIENT_TIME'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (clientTime != null) out['HAPPY_WAKEY_CLIENT_TIME'] = clientTime;
  final event = pick(['HAPPY_WAKEY_TRANSITION_EVENT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (event != null) out['HAPPY_WAKEY_TRANSITION_EVENT'] = event;
  final expectedGeneration = pick(['HAPPY_WAKEY_EXPECTED_GENERATION'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (expectedGeneration != null) out['HAPPY_WAKEY_EXPECTED_GENERATION'] = expectedGeneration;
  final occurrenceId = pick(['HAPPY_WAKEY_OCCURRENCE_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (occurrenceId != null) out['HAPPY_WAKEY_OCCURRENCE_ID'] = occurrenceId;
  final snoozeUntil = pick(['HAPPY_WAKEY_SNOOZE_UNTIL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (snoozeUntil != null) out['HAPPY_WAKEY_SNOOZE_UNTIL'] = snoozeUntil;
  return out;
}

const List<String> _dotenvFiles = ['.env'];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(Platform.environment, loadDotenvFiles(_dotenvFiles));
}
